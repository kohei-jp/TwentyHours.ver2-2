.contents.row.initial
  %p.user_page
    = @name 
    さんの投稿一覧
.contents.row
  - @tags.each do |t|
    = t.tag_name

  -# - @tweets.each do |tweet|
  -#   = render partial: "tweets/tweet", locals: { tweet: tweet}
.contents.row.initial
  = paginate(@tweets)
